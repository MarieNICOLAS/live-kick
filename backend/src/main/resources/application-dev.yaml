spring:
  datasource:
    url: jdbc:sqlite:${LIVEKICK_SQLITE_PATH:livekick.db}
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1

logging:
  level:
    com.livekick: DEBUG
